class Generate {
    private int: number;

    private string: string;

    /*
     * We don't have a constructor here. Invoke `Generate...`, then select Constructor and hit Enter. Select both properties with Shift and hit Enter again. Voilà!
     * Now invoke `Generate...` again, this time select Getter. Again, with Shift select both properties and hit Enter. You can see that not only the getters were generated, but the property names were updated not to generate a TS error.
     *
     * The code being generated is most likely customizable, although I'm not sure where.
     */
}
