class Run {
    constructor() {
        console.log('In a project where you have some tests, navigate to one of the tests and invoke `Run...`. The test will run inside Phpstorm, or you might be presented with "Edit configurations" dialog (see https://www.jetbrains.com/help/phpstorm/run-debug-configuration.html for help.)');
        console.log('This is compatible with multiple JS test frameworks and PHP test frameworks, but you can also add a custom run configuration for practically anything e.g. using a shell, so you can quickly trigger a phpstan analysis for example.');
    }
}
